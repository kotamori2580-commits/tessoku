#include <bits/stdc++.h>
using namespace std;

priority_queue<int, vector<int>, greater<int>> A[100009];
vector<int> B[100009];

int main(){
  int n;
  for (int i=1;i<=n;i++){
    cin >> A[i];
  }
  for (int i=1;i<=n;i++){
    int a;
    a = A.top();
    for (int j=1;j<=n;j++){
      if (A[j] ==a){
        B[j] == i;
      }
      A[j] = 1000000;
    }
  }  
}
